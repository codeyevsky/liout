# built binary
/liout

# user runtime data (generated by `liout init` / at runtime)
/config.json
/session.json
/rules.json
/blacklist.txt
/leads*.csv
/filtered.csv
/data/
/messages/
/dump.json

# browser profiles live under XDG data dir, but ignore any local copy
/profiles/

# OS / editor cruft
*.log
.DS_Store
